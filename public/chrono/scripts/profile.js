'use strict';

if (typeof ProfileMenu === 'undefined') {
	var ProfileMenu = {
		init: function () {},
		open: function () {},
		close: function () {}
	};
}

if (typeof LogoutDialog === 'undefined') {
	var LogoutDialog = {
		open: function () {},
		close: function () {},
		confirm: function () {}
	};
}
